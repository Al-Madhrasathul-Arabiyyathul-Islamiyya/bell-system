#!/usr/bin/env bash
set -e

cd "$(git rev-parse --show-toplevel)/bell-system-backend"

echo "==> Running tests..."
go test ./... -count=1

echo "==> Verifying build..."
go build ./cmd/server

echo "==> Pre-push checks passed."
